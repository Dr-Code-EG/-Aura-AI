"""Dr Code — click-to-run desktop exam assistant.

The user clicks one button (or presses a global hotkey), the app
captures the current screen and asks ChatGPT (via an embedded
logged-in browser session) to answer the question that's on screen.
The answer is shown inline in a floating panel that stays on top of
the exam window.
"""

__version__ = "1.0.0"
