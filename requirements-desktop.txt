# Dependencies for the simplified Aura Desktop build (desktop_app/).
# Install with:  pip install -r requirements-desktop.txt
# This is a separate, lighter-weight install from the main `requirements.txt`,
# which is for the original pywebview/FastAPI stealth overlay.

PyQt6>=6.6
PyQt6-WebEngine>=6.6
mss>=9.0
Pillow>=10.0
httpx>=0.27
pynput>=1.7
